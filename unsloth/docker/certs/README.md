# docker/certs/

Сюда — `*.crt` корневых CA сети с подменой TLS-сертификатов (см. `../../README.md`,
«Быстрый старт в сети с подменой TLS-сертификатов»). В git не коммитятся (.gitignore).
Детали: `../../notes/tls_and_hf_downloads.md`.
